{
  "name": "fgmpack-protocol",
  "version": "0.1.0",
  "description": "Shared protocol types for the Fragment Pack language server and clients",
  "main": "../dist/protocol/index.js",
  "types": "../dist/protocol/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.5.2",
    "jest": "^30.1.3",
    "ts-jest": "^29.4.4",
    "typescript": "^5.9.2"
  }
}
