{
  "name": "fragments-protocol",
  "version": "0.1.0",
  "description": "Shared protocol types for the Fragments language server and clients",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
